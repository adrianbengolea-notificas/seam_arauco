# Copiá a .env para emulador local (opcional). En producción usá Secret Manager + params (ver README).
CRON_TARGET_URL=https://tu-app.web.app
# CRON_SECRET lo define Secret Manager al hacer: firebase functions:secrets:set CRON_SECRET
